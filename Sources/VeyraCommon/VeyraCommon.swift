// VeyraCommon — STORY-33 Phase 0 placeholder.
// The public Swift facade over the veyra-kernel KMP framework lands in Phase 2
// (story-33 §2.4 naming: Swift-idiomatic, no Iventure* prefix).
public enum VeyraCommonInfo {
    /// Version tracks the shared release train (gradle.properties VERSION_NAME).
    public static let phase = "STORY-33 Phase 0 skeleton"
}
